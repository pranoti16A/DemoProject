phases:
  install:
    commands:
      - echo "install phase..."
  Pre_build:
   commands:
      - echo "pre build phase..." 
      - apt-get update
      - apt-get install -y jq
      - wget https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-4.7.0.2747-linux.zip
      - echo "current path is >>>"
      - sh $pwd
      - unzip ./sonar-scanner-cli-4.7.0.2747-linux.zip
      - ls $pwd
      - export PATH=$PATH:./sonar-scanner-4.7.0.2747-linux/bin/
  build:
    commands:
      - echo "build phase"
      - sonar-scanner -Dsonar.login="3bb96c3ebe7a0437f70c45a984ccedf70eeda59c" -Dsonar.host="https://sonarcloud.io" -Dsonar.projectkey="pranoti16A_DemoProject" -Dsonar.organization="pranoti16a"
      - sleep 5
      - curl https://sonarcloud.io/api/qualitygates/project_status?projectkey=pranoti16A_DemoProject >result.json
      - cat result.json

